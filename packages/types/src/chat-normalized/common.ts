export type ChatNormalizedEventSource = "gateway" | "synthetic";

export type ChatNormalizedCorrelationConfidence = "exact" | "fallback";

export type ChatNormalizedApprovalDecision = "allow-once" | "allow-always" | "deny";
