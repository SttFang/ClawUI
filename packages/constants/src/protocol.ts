/** ACP protocol version that ClawUI requires. Event parsing is based on v3. */
export const ACP_PROTOCOL_VERSION = 3;

/** Timeout for ACP request calls (ms). */
export const REQUEST_TIMEOUT_MS = 30_000;

/** Timeout for ACP connect handshake (ms). */
export const CONNECT_TIMEOUT_MS = 10_000;
