/** Default OpenClaw gateway port. */
export const DEFAULT_GATEWAY_PORT = 18789;

/** Rescue gateway port (config-agent profile). */
export const RESCUE_GATEWAY_PORT = 19789;

/** OpenClaw profile name for the config agent. */
export const CONFIG_AGENT_PROFILE_NAME = "clawui-config-agent";

/**
 * ClawUI 要求的最低 OpenClaw 版本。
 * Installer 和 RuntimeDetector 共享此常量。
 */
export const MIN_OPENCLAW_VERSION = "2026.2.26";
